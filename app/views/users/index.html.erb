<% provide(:title, 'All users') %>

<h2>User Accounts</h2>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= link_to "Add a User!", signup_path, class: "btn btn-large btn-secondary" %>